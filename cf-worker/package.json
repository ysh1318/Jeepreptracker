{
  "name": "jee-edofox-sync-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Edofox login/scrape/encrypt sync worker — replaces the Firebase Cloud Functions version (functions/src/index.ts) so this doesn't require Firebase's paid Blaze plan.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set-key": "wrangler secret put EDOFOX_ENC_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.4.0",
    "@cloudflare/workers-types": "^4.20241106.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "jose": "^5.9.6"
  }
}
