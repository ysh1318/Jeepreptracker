// Seeding function is disabled to prevent fake data entry.
export async function bootstrapDatabase() {
  console.log("Database seeding is disabled.");
}

export const SEED_STUDENTS: any[] = [];
export const SEED_MOCK_SCORES: any = {};
export const SEED_STUDENT_LOGS: any = {};
